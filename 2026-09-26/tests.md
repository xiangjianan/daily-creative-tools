# 留住 · 测试记录

2026-09-26，Node规则测试与Codex In-app Browser实际交互。

## 规则

`node --test 2026-09-26/core.test.mjs`：7/7通过。含示例模式、边缘主体、高主体留白、恰好容纳、无效输入、等比居中，以及900组尺寸/比例/保护框组合的几何不变量：裁切不出原图且完整包含保护框。

## 浏览器实测

- 空页无下载结果；点示例生成三张：横版裁切、方形/竖版留白。
- 真正点击原图两角，输入同步，结果由三处留白变为一处；百分比移动到右上圆形区域后三个均可裁切。
- 左80右70被拦截；键盘清空左边报错，下载消失；保留整图可恢复。
- 实际点击下载竖版，文件落盘`留住-900x1200.png`。sips核实900×1200，63320字节；打开图确认文字完整、居中留白、无保护框叠层。浏览器download事件等待超时，但实际文件成功落盘，因此以磁盘及图像检查作证，没有把事件超时误判为成功或失败。
- 通过文件选择器重新读入上述PNG，显示900×1200并生成三种结果。
- 选择伪PNG（内容为普通文本）提示无法读取，旧结果被清空；示例可恢复。
- 1280×720桌面双列截图、390×844手机单列截图检查通过；390和320宽度下文档宽度均不超过视口。手机视口可修改四个输入并恢复整图。
- console error/warn为空。

## 限制

未测Safari/Firefox、真实手机触摸/系统照片选择器、JPEG的EXIF旋转、动画WebP、接近容量上限性能与色彩管理；不声称无损或平台规格适配。颜色输入有实现但未逐个颜色进行像素校验。实际文件落盘核验覆盖竖版，另外两种已核对Canvas尺寸及下载链接生成。

## 发布

待Pages部署后追加线上复验。
