# 齐件：真实测试记录

日期：2026-09-17。规则测试：Node.js v22.22.3 内建 node:test；界面测试：Codex In-app Browser，实际浏览器页面。

## 自动化规则测试

`node --test 2026-09-17/core.test.mjs`：12 / 12 通过。

覆盖：完整交付；示例缺一项/两个版本/清单外文件；同一文件命中两项；零字节；空输入；全角英文与项目符号；重复关键词与单字；60 项/200 文件/12,000 字限制；扩展名字面匹配；同名重复文件；清单外阻止“齐件”；小票文本包含状态、示例标识和边界。

## 本地浏览器实测

| 操作 | 实际观察 |
| --- | --- |
| 首次打开空页面 | 显示先写清单，复制按钮禁用 |
| 点击“试试示例” | 2/4 已对应；缺使用说明、报价单需选一份、内部讨论记录清单外 |
| 移除报价 v1 与内部记录 | 3/4 已对应，仍缺说明；即时更新 |
| 点击复制 | 页面显示复制成功；浏览器剪贴板读取接口返回空，未宣称跨应用粘贴已验证。小票内容由规则测试独立校验 |
| 通过文件选择器多选 4 个本地测试文件 | 示例被替换，显示“本地核对”、4/4 已对应、“文件名都对上了” |
| 改清单为方案 / 项目方案 | 两项均为匹配重叠，没有把一文件算两份 |
| 改为两个相同关键词 | 报重复，复制禁用 |
| 清空清单 | 恢复提示，复制禁用 |
| 选择真实 0B 测试文件 | 空文件提示，0/1 已对应，待确认 |
| 单字与 61 项清单 | 对应限制错误，没有截断计算 |
| 文件名含 `<img src=x onerror=alert(1)>.txt` | 字面显示，无 img 节点、无 alert，未作为 HTML 执行 |
| 桌面 1280×720 | 实际截图检查双列、文字和按钮，无重叠 |
| 390×844 手机视口 | 实际截图检查输入区与结果区，单列，无横向溢出；复制按钮可操作 |
| 320×740 窄视口 | DOM 宽度 320，未横向溢出 |
| 控制台 | warnings/errors 列表为空 |

测试文件全部是临时合成数据，扩展名只用于文件名核对；未用实际业务文档。已修复 HTML placeholder 中字面 `\n` 未换行的问题，并重新加载验证。

## 测试边界

- 已用真实文件选择器完成核心导入流程；未做 Finder 原生跨窗口拖放、文件夹拖放与 200 份文件的端到端测试；其处理规则做了代码检查，数量上限做了规则测试。
- 未覆盖 Safari、Firefox 和 iOS/Android 真机；手机结果为浏览器视口测试。
- 自动复制成功提示已观察；拒绝剪贴板时的手动复制回退未强制模拟。
- 未读取文件内容、未做格式有效性/内容正确性检查；未检验压缩包内部。
- 不记录、不上传清单或文件信息；静态代码检查没有 fetch、XHR、FileReader、文件 text/arrayBuffer、localStorage 或第三方资源；CSP connect-src 为 none。

## 部署验证

初次发布进行中；完成后在此追加实际部署及线上核心功能验证。
